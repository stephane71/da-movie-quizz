import {
  gameTimer
} from '../../../helpers/game-timer';
import { module, test } from 'qunit';

module('GameTimerHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = gameTimer(42);
  assert.ok(result);
});
